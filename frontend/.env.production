# Production config — used automatically during `npm run build` on Vercel.
# Left blank on purpose: the frontend and the API live on the SAME Vercel
# domain, so requests go to /api/chat (same origin). No separate backend URL.
VITE_API_URL=
